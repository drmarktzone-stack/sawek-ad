# Optional. The app runs with none of these.

# Optional AI Studio key. Vertex on the GCP pack is preferred and tried first.
GEMINI_API_KEY=

# Optional campaign sync. localStorage is the default.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Social OAuth (Facebook Login / Instagram Graph, LinkedIn). Never commit real values.
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
TOKEN_ENCRYPTION_KEY=
SUPABASE_SERVICE_ROLE_KEY=
APP_BASE_URL=https://sawek-ad-308665814452.me-west1.run.app

# Vertex AI Gemini (text) + Imagen (images). Same $300 Google Cloud pack.
# Default project in code: project-8fd8a005-ae6d-4139-ab4. Cloud Run uses the compute SA.
GOOGLE_CLOUD_PROJECT=project-8fd8a005-ae6d-4139-ab4
VERTEX_LOCATION=us-central1

# Stripe Checkout (runtime only — never commit real keys).
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_MONTHLY=
STRIPE_PRICE_YEARLY=

# Optional PayPal.me if Stripe PayPal is off. Example: https://paypal.me/yourname
PAYPAL_ME=

# Bank / Bit instructions shown on /billing/bank. Leave empty — do not invent IBAN.
BANK_INSTRUCTIONS=
BIT_INSTRUCTIONS=
